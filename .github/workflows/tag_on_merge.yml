run: |
  TAG_NAME="v$(date +'%Y%m%d%H%M%S')"  # genera v20250409154000
  git config user.name "github-actions"
  git config user.email "github-actions@github.com"
  git tag "$TAG_NAME"
  git push origin "$TAG_NAME"
